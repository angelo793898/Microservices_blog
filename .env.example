# PostgreSQL Database Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the real .env file to git!

# External PostgreSQL connection settings
# For local development only - Kubernetes uses secrets
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_actual_password_here

# Setup Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace 'your_actual_password_here' with your real PostgreSQL password
# 3. Install PostgreSQL locally on your machine
# 4. Ensure PostgreSQL is running on port 5432
# 5. For Kubernetes deployment, create the secret manually:
#    kubectl create secret generic database-secret \
#      --from-literal=host=host.docker.internal \
#      --from-literal=port=5432 \
#      --from-literal=name=postgres \
#      --from-literal=user=postgres \
#      --from-literal=password=YOUR_REAL_PASSWORD

# SECURITY NOTES:
# - The .env file is in .gitignore and will not be committed
# - Kubernetes deployments use secrets, not this file
# - Never share your real database password in code or chat